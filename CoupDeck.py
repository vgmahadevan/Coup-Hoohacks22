import random

class CoupDeck:
    def __init__():
        self.deck = [0,1,2,3,4]*3
        deck.shuffle()

    def shuffle():
        self.deck.shuffle()

    def draw():
        return deck.pop()

    def add(card1, card2 = -1):
        self.deck.append(card1)
        if card2 != -1:
            self.deck.append(card2)
        self.deck.shuffle()